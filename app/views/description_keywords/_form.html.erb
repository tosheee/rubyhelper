<%= form_for(@description_keyword) do |f| %>
  <% if @description_keyword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@description_keyword.errors.count, "error") %> prohibited this description_keyword from being saved:</h2>

      <ul>
      <% @description_keyword.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.collection_select(:syntax_keyword_rubies_id, SyntaxKeywordRuby.all, :id, :syntax_keyword, :prompt => 'Select Keyword')%>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_area :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
