<%= form_for(@syntax_elements_ruby) do |f| %>
  <% if @syntax_elements_ruby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@syntax_elements_ruby.errors.count, "error") %> prohibited this syntax_elements_ruby from being saved:</h2>

      <ul>
      <% @syntax_elements_ruby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :element %><br>
    <%= f.text_field :element %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
